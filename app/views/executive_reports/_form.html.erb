<%= form_with(model: executive_report) do |form| %>
  <% if executive_report.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(executive_report.errors.count, "error") %> prohibited this executive_report from being saved:</h2>

      <ul>
        <% executive_report.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
